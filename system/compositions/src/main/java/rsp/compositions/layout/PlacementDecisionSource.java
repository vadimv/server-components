package rsp.compositions.layout;

/**
 * Explains where an effective placement decision came from.
 */
public enum PlacementDecisionSource {
    LAYOUT_PLACEMENT,
    GROUP_PLACEMENT_POLICY,
    FRAMEWORK_DEFAULT
}
