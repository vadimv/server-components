package rsp.http;

/**
 * Result selected by a synchronous HTTP route.
 *
 * <p>{@link HttpResponse} is the transport-ready result. Higher-level server
 * adapters may define additional results that they know how to render.</p>
 */
public interface HttpResult {
}
